# Shared API-Lambda layer dependencies. Stdlib only today (json/os/functools); boto3 is provided by
# the Lambda runtime and is not used here anyway. Listed so build.sh has a consistent place to vendor
# from per layer. Test-only deps live in backend/tests/requirements-dev.txt.

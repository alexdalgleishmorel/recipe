# Backend test dependencies (not vendored into the Lambda bundle — build.sh only vendors the
# functions'/layers' own requirements.txt). boto3 is provided by the Lambda runtime in prod but is a
# direct dependency of the data-access layer here; moto mocks DynamoDB so tests need no real AWS.
boto3>=1.34
moto[dynamodb]>=5.0
pytest>=8.0

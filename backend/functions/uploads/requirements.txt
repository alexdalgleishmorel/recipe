# Runtime dependencies for the uploads handler. None today — boto3 is provided by the Lambda runtime,
# and the common layer is bundled by build.sh. Listed so build.sh has a consistent place to vendor
# deps from per function.
